const Heading = () => {
    return (
        // Render an <h1> element with the following styling
        <h1 
            className="border border-white rounded-[50px] text-7xl text-center shadow-[0px_0px_15px_rgba(0,0,0,0.25)] mt-[15%] mb-[8%] w-[700px] bg-[#F0A8D0]"
        >
            To do list  {/* Text displayed in the heading */}
        </h1>
    );
}

export default Heading;  // Export the Heading component as the default export
